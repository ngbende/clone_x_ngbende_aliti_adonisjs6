<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
      {{ title || "Your default title" }}
    </title>
    <script src="https://cdn.tailwindcss.com"></script>

    @if ($slots.meta)
      {{{ await $slots.meta() }}}
    @endif
    
  </head>
  <body class="bg-black flex flex-col w-full min-h-screen px-4">
    {{{ await $slots.main() }}}
  </body>
</html>