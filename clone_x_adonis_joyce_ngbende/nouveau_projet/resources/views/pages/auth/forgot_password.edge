<!doctype html>
<html>
<head>
  <title>Mot de passe oublié</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black min-h-screen flex items-center justify-center">
  <div class="max-w-md w-full bg-gray-900 p-8 rounded-lg shadow-lg">
    <h1 class="text-2xl font-bold text-white mb-6">Mot de passe oublié</h1>
    
   @if(flashMessages.has('error'))
  <div class="bg-red-500 text-white p-3 rounded mb-4">
    {{ flashMessages.get('error') }}
  </div>
@endif

@if(flashMessages.has('success'))  
  <div class="bg-green-500 text-white p-3 rounded mb-4">
    {{ flashMessages.get('success') }}
  </div>
@endif

    <form method="POST" action="{{ route('password.email') }}">
      {{ csrfField() }}
      
      <div class="mb-4">
        <label class="block text-white mb-2">Email</label>
        <input type="email" name="email" required 
               class="w-full p-3 bg-gray-800 text-white rounded border border-gray-700">
      </div>

      <button type="submit" 
              class="w-full bg-blue-500 text-white p-3 rounded hover:bg-blue-600">
        Envoyer le lien de réinitialisation
      </button>
    </form>

    <div class="mt-4 text-center">
      <a href="{{ route('show.login') }}" class="text-blue-400 hover:underline">
        Retour à la connexion
      </a>
    </div>
  </div>
</body>
</html>